{
  "name": "@elithrar/workers-hono-rate-limit",
  "version": "0.2.0",
  "description": "Hono-compatible middleware for rate limiting requests with Cloudflare Workers.",
  "exports": {
		".": {
      "types": "./dist/index.d.mts",
			"import": "./dist/index.mjs",
			"require": "./dist/index.mjs"
		}
	},
  "main": "dist/index.mjs",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest --run",
    "build": "npx unbuild"
  },
  "license": "Apache-2.0",
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/elithrar/hono-cf-rate-limit.git"
  },
  "homepage": "https://github.com/elithrar/hono-cf-rate-limit",
  "peerDependencies": {
    "hono": "*"
  },
  "devDependencies": {
    "hono": "^3.11.7",
    "unbuild": "^2.0.0",
    "vitest": "^1.0.4"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240405.0",
    "jest": "^29.7.0"
  }
}
